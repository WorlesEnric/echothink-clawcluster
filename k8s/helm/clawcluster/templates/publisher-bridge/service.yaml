{{- $fullname := include "clawcluster.fullname" . -}}
{{- $namespace := include "clawcluster.namespace" . -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ $fullname }}-publisher-bridge
  namespace: {{ $namespace }}
  labels:
    {{- include "clawcluster.componentLabels" (dict "ctx" . "component" "publisher-bridge") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.publisherBridge.service.port }}
      targetPort: http
  selector:
    {{- include "clawcluster.componentSelectorLabels" (dict "ctx" . "component" "publisher-bridge") | nindent 4 }}
