{{- $fullname := include "clawcluster.fullname" . -}}
{{- $namespace := include "clawcluster.namespace" . -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ $fullname }}-hiclaw-manager
  namespace: {{ $namespace }}
  labels:
    {{- include "clawcluster.componentLabels" (dict "ctx" . "component" "hiclaw-manager") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.hiclawManager.service.port }}
      targetPort: http
  selector:
    {{- include "clawcluster.componentSelectorLabels" (dict "ctx" . "component" "hiclaw-manager") | nindent 4 }}
