{{- $fullname := include "clawcluster.fullname" . -}}
{{- $namespace := include "clawcluster.namespace" . -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ $fullname }}-element-web
  namespace: {{ $namespace }}
  labels:
    {{- include "clawcluster.componentLabels" (dict "ctx" . "component" "element-web") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.elementWeb.service.port }}
      targetPort: http
  selector:
    {{- include "clawcluster.componentSelectorLabels" (dict "ctx" . "component" "element-web") | nindent 4 }}
