{{- $fullname := include "clawcluster.fullname" . -}}
{{- $namespace := include "clawcluster.namespace" . -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ $fullname }}-policy-bridge
  namespace: {{ $namespace }}
  labels:
    {{- include "clawcluster.componentLabels" (dict "ctx" . "component" "policy-bridge") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.policyBridge.service.port }}
      targetPort: http
  selector:
    {{- include "clawcluster.componentSelectorLabels" (dict "ctx" . "component" "policy-bridge") | nindent 4 }}
